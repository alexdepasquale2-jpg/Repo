import { test } from '@playwright/test';

/**
 * PlayMode equivalent: the Power pillar, and the price it pays in Dread.
 *
 * The rule under test is the coupling: ghosts are free and silent, solidifying costs materials AND
 * makes noise. Every one of these is fixme until the building loop exists.
 */
test.describe('building and solidifying', () => {
  test.fixme('placing a ghost costs nothing and makes no noise', () => {});
  test.fixme('solidifying deducts materials up front', () => {});
  test.fixme('solidifying emits noise that reaches the loudness meter', () => {});
  test.fixme('cancelling mid-solidify does not refund the materials already spent', () => {});
  test.fixme('scrapping a solidified piece refunds less than it cost', () => {});
  test.fixme('a piece destroyed by enemies refunds nothing', () => {});
  test.fixme('a module stamp produces one loudness spike rather than several small ones', () => {});
  test.fixme('a loaded blueprint places ghosts without charging anything', () => {});
  test.fixme('the fort does not exist after the run resolves', () => {});
});
